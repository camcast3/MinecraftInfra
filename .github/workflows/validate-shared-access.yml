---
name: Validate shared access files

on:
  pull_request:
    paths:
      - "docker/shared/whitelist.json"
      - "docker/shared/ops.json"
      - "docker/shared/**"
      - "games/minecraft/shared/**"
      - "scripts/sync-ops.ps1"
      - "tools/access/sync-ops.ps1"
      - "tools/layout/**"
      - ".github/workflows/validate-shared-access.yml"
  push:
    branches: [main]
    paths:
      - "docker/shared/whitelist.json"
      - "docker/shared/ops.json"
      - "docker/shared/**"
      - "games/minecraft/shared/**"
      - "scripts/sync-ops.ps1"
      - "tools/access/sync-ops.ps1"
      - "tools/layout/**"
      - ".github/workflows/validate-shared-access.yml"

permissions:
  contents: read

jobs:
  ops-match-whitelist:
    name: ops.json matches whitelist.json
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - name: Check generated operators
        shell: pwsh
        run: |
          ./tools/access/sync-ops.ps1 -Check
          ./tools/layout/Test-RepositoryLayout.ps1
