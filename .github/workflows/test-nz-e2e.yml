name: nz CLI E2E tests

on:
  pull_request:
    paths:
      - "client/**"
      - "games/minecraft/client/**"
      - "packwiz/**"
      - "docs/**/*.md"
      - "docs/assets/*.ps1"
      - "infra/azure/scripts/build-instance-from-packwiz.ps1"
      - "infra/azure/scripts/publish-prism-pack.ps1"
      - "infra/azure/scripts/promote-prism-pack.ps1"
      - "scripts/Test-ClientDistribution.ps1"
      - "scripts/Test-MinecraftStatus.ps1"
      - "tools/client/**"
      - "tools/layout/**"
      - ".github/workflows/**"
  push:
    branches: [main]
    paths:
      - "client/**"
      - "games/minecraft/client/**"
      - "packwiz/**"
      - "docs/**/*.md"
      - "docs/assets/*.ps1"
      - "infra/azure/scripts/build-instance-from-packwiz.ps1"
      - "infra/azure/scripts/publish-prism-pack.ps1"
      - "infra/azure/scripts/promote-prism-pack.ps1"
      - "scripts/Test-ClientDistribution.ps1"
      - "scripts/Test-MinecraftStatus.ps1"
      - "tools/client/**"
      - "tools/layout/**"
      - ".github/workflows/**"

permissions:
  contents: read

concurrency:
  group: nz-e2e-${{ github.ref }}
  cancel-in-progress: true

jobs:
  e2e:
    name: Full nz compatibility gate
    runs-on: windows-latest
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 1

      - uses: actions/setup-go@v7
        with:
          go-version-file: games/minecraft/client/go.mod
          cache-dependency-path: games/minecraft/client/go.sum

      - name: Show toolchain
        run: go version
        working-directory: games/minecraft/client

      - name: Unit tests and vet
        shell: pwsh
        working-directory: games/minecraft/client
        run: |
          go test ./...
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }
          go vet ./...
          if ($LASTEXITCODE -ne 0) { exit $LASTEXITCODE }

      - name: Run e2e harness
        run: go test -tags e2e -v -timeout 10m ./tests/e2e/
        working-directory: games/minecraft/client

      - name: Run immutable corpus compatibility
        shell: pwsh
        run: ./games/minecraft/client/scripts/Test-InstanceCorpus.ps1

      - name: Validate disposable package
        shell: pwsh
        run: ./games/minecraft/client/scripts/Test-Packaging.ps1

      - name: Validate client distribution and docs paths
        shell: pwsh
        run: |
          ./tools/layout/Test-RepositoryLayout.ps1
          ./tools/client/Test-ClientDistribution.ps1
