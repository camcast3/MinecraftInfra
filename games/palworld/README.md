# Palworld

`docker-compose.yml` is the game-owned source. Portainer remains configured for
`docker/palworld/docker-compose.yml`, which is an equality-checked compatibility copy.
Repoint only after the live promotion checklist in `ops/multigame-rollout-checklist.md`
is complete.

