# Disable Git line ending conversion for most files (prevents packwiz index hash changes on Windows)
* -text

# Shell scripts must always have LF line endings or they fail on Linux
*.sh text eol=lf