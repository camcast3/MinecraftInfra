setcommands.sh
admin_compose.yml

# Exclude exported CurseForge zip files
*.zip

# Exclude exported Modrinth modpacks
*.mrpack
infra/azure/main.json
checklist.md

# Runtime secrets — never commit
.env
!.env.example

# Local admin runbook, not for repo
checkList_onboarding.md
checklist_onboarding.md
