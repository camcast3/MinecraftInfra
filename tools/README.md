# Tools

Repository automation is grouped by purpose:

- `access/` — generated Minecraft access data;
- `client/` — client distribution and server compatibility gates;
- `layout/` — ownership/compatibility synchronization and validation;
- `validation/` — contracts and multi-game rollout validation.

Forwarding wrappers remain under `scripts/` so existing local commands and workflows
continue to work during the migration.

