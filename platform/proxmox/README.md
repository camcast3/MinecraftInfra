# Proxmox platform

`game-node/` owns the reusable Palworld/Windrose Debian template and backup tooling.
`minecraft/cloud-init.yaml` mirrors the existing C2E2 provisioning file.

The compatibility copies under `infra/proxmox/` remain in place for current raw URLs
and provisioning instructions.

